# PR #116

- Preserves the original durable RLUSD receipt when the same validated transaction is re-verified at a later observation time.
- Keeps different-transaction conflicts fail-closed while allowing exact paid-slot retries to return `receipt.status = existing`.
- Adds a regression test that reproduces the live Mainnet retry with a changed `verifiedAt` timestamp and an unchanged stored receipt digest.
