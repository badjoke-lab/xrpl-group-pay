# PR #89 changelog fragment

- Defines the controlled Mainnet XRP acceptance contract and public-safe evidence boundary.
- Adds a readiness validator for the current release stage, halted production baseline, isolated Mainnet D1 binding, custom domain, Source Tag, XRP Asset identity, account format, and 1–1,000 drop limit.
- Adds regression coverage that rejects stale release stages, previously accepted XRP evidence, unsafe amounts, invalid accounts, enabled baseline operations, and shared production databases.
- Documents the participant-controlled Xaman signature boundary, duplicate and cross-slot replay controls, mandatory rollback, and separate evidence-import review.
- Does not create a Xaman request, sign or submit a transaction, enable Mainnet payment operations, or change the blocked release decision.
