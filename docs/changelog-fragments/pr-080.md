# PR #80 changelog fragment

- Includes all Mainnet script tests in Vitest instead of only the Xaman attestation tests.
- Allows the committed production target to advance from closed preparation to an internal halted deployment while the overall release remains blocked.
- Validates the imported halted deployment state through evidence, acceptance, release-plan, production-target, and configuration checks.
