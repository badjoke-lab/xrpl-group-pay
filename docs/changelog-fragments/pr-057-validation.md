# PR #57 validation note

The Mainnet acceptance decision is intentionally blocked. Normal validation must confirm consistency without opening Mainnet, while `deploy:mainnet` must fail until `--require-ready` succeeds.
