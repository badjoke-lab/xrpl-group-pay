# PR #63 changelog fragment

## Added

- A safe Mainnet Xaman `SignIn` attestation request shape that forces `MAINNET`, expires after one minute, and cannot submit an XRPL transaction.
- A strict public-safe report contract for production Xaman provider attestation.
- A fail-closed importer for verified provider attestation reports.
- Unit and integration coverage for stale, incomplete, foreign, sensitive, conflicting, inconsistent, and idempotent report handling.
- An operator runbook for the later credential and callback verification step.

## Import boundary

A successful import may update only:

- the `production-provider-attestation` release-evidence record;
- the matching acceptance control;
- the matching `production-provider-not-attested` finding.

The importer preserves the blocked release decision and leaves every unrelated Mainnet finding unchanged.

## Release state

- No Xaman credential was used by this pull request.
- No remote Xaman payload was created.
- No provider evidence was accepted.
- Mainnet runtime remains disabled and payment operations remain halted.
