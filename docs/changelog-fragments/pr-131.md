# PR #131

- Reloads the page after a language change so server-rendered page copy and client-rendered form copy cannot remain in different languages.
- Makes the production Bill UI audit wait for the complete Japanese server-rendered shell after changing language.
- Requires the reviewed Japanese form headings and rejects the previous literal phrases.
- Fails when a Japanese RLUSD capture still contains the English creator shell, stale Japanese copy, the wrong asset, or horizontal overflow.
- Does not create a Bill, request a wallet signature, submit a transaction, or move funds.
