# PR #108

- Reconciles validated XRPL account history before replacing any prior Wallet Handoff.
- Searches the exact expected payer and frozen InvoiceID through bounded, marker-aware `account_tx` reads on the PaymentSlot network.
- Independently verifies every candidate against the frozen destination, Asset, amount, Source Tag, Destination Tag, InvoiceID, payer, result, and delivered amount.
- Settles one exact validated match idempotently and refuses a replacement request.
- Persists a public-safe reconciliation finding and moves the PaymentSlot and Bill to `needs_review` when multiple exact validated matches exist.
- Fails closed when account history is unavailable, unvalidated, cross-network, malformed, or incomplete within the bounded pagination window.
- Adds migration, API, history-client, reconciliation, and fail-closed tests.
- Does not enable Mainnet operations, deploy Cloudflare resources, or accept the failed RLUSD ceremony as release evidence.
