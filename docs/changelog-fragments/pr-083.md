# PR #83 changelog fragment

- Uses OpenNext only for the Cloudflare build transform.
- Deploys the generated Worker with Wrangler directly so `--secrets-file` is handled by Wrangler itself.
- Keeps the internal halted runtime contract and public verification unchanged.
