# PR #91 changelog fragment

- Adds a manual, non-executing readiness Action for the controlled XRP acceptance stage.
- Validates the proposed destination, payer, and 1–1,000 drop amount against the committed halted production state.
- Uses read-only repository permissions and performs no deployment or production write.
- Adds regression coverage for the workflow boundary.
