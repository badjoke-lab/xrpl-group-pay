# PR #82 changelog fragment

- Passes Wrangler deployment options directly to the OpenNext deploy command.
- Removes the extra argument separator that prevented `--secrets-file` from reaching Wrangler correctly.
- Adds explicit sanitized deployment stage markers to future workflow logs.
