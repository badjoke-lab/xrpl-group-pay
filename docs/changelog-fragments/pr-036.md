# PR #36 changelog fragment

## Changed

- XRP settlement now writes canonical Asset identity, generic units, and the versioned verified-payment digest into the legacy receipt row.
- D1 triggers synchronize new receipts and legacy digest upgrades into `verified_payment_records` within the existing four-statement settlement batch.

## Compatibility

- The existing XRP receipt response and legacy proof digest remain unchanged.
- Exact retries remain idempotent with `changes = 0` after a canonical record exists.
- RLUSD settlement still uses the independent generic recorder until the PaymentSlot state switch is merged.
