# PR #61 changelog fragment

## Fixed

- The Mainnet D1 workflow now writes its generated Wrangler configuration beside the repository `migrations/` directory instead of under the runner temporary directory.
- Wrangler therefore resolves `migrations_dir: "migrations"` against the checked-out repository and can apply the versioned D1 migrations.
- The generated configuration is removed with an `always()` cleanup step after the remote operation.

## Incident context

- Inspect run `28214240202` completed successfully and confirmed that no protected production or preview database existed at that time.
- Provision run `28214314389` reached the remote D1 operation and failed before producing an artifact because the generated configuration made the migration directory resolve outside the repository.
- A retry may reuse any exact protected-name database created before the failed run; the workflow must not delete it merely because the first migration attempt failed.

## Release state

- This pull request does not provision, import evidence, deploy the Worker, or enable Mainnet.
- Mainnet remains disabled, operationally halted, and blocked pending a successful rerun.
