# PR #32 changelog fragment

## Added

- Official XRPL RLUSD Asset descriptors for Testnet and Mainnet.
- Network-specific issuer constants and the canonical 40-character RLUSD currency code.
- A dedicated RLUSD Asset lookup that preserves currency and issuer identity.

## Boundary

- The configured scale is an application allocation and persistence scale, not an XRPL protocol decimal declaration.
- RLUSD payment creation remains disabled until the issued-asset builder and verifier are merged.
