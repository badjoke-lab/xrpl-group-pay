# PR #119

- Adds a manually confirmed GitHub Actions workflow for the first public Mainnet operating deployment.
- Generates `public + enabled` runtime configuration only after the repository gate, final audit, acceptance controls, and all seven evidence records are approved.
- Verifies the public payment-status endpoint and guarded Xaman callback after deployment.
- Automatically restores the reviewed `internal + halted` target if post-deployment verification fails.
- Keeps secrets, wallet handoff material, signed transactions, and private deployment output out of repository evidence.
