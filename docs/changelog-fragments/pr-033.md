# PR #33 changelog fragment

## Added

- RLUSD Payment Intent creation with official network-specific Asset identity.
- An XRPL issued-asset Payment builder using `currency`, `issuer`, and decimal-string `value`.
- Payment builder dispatch for native and issued Assets.

## Boundary

- Internal fixed-precision units are converted without JavaScript floating-point arithmetic.
- Xaman's public provider capability remains XRP-only until issued-payment verification is merged.
