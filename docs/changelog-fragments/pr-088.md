# PR #88 changelog fragment

- Makes frozen Bill creation deployment-network-aware for controlled Mainnet acceptance.
- Adds canonical Mainnet XRP and official Mainnet RLUSD Bill input identities while keeping legacy XRP input Testnet-only.
- Resolves the D1 database binding and deployment network together so cross-network Bills fail before persistence.
- Persists the resolved Bill network instead of a Testnet constant.
- Adds Mainnet Bill, API identity, D1 isolation, and cross-network rejection coverage.
- Keeps the Mainnet Worker internal, the final release decision blocked, and payment operations halted.
