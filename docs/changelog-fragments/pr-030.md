# PR #30 changelog fragment

## Changed

- PaymentSlot loading now requires the versioned Asset identity and fixed-precision amount stored in D1.
- The compatibility `expectedAmountDrops` value used by the current XRP flow is sourced from generic amount units.

## Compatibility

- Current XRP Testnet Payment Intents and wallet payloads remain unchanged.
- Older in-memory test fixtures may continue using the legacy compatibility field during the transition.
